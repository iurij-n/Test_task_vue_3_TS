# Тестовое задание соискателя для вакинсии "Middle+/Senior Front-end разработчик (Vue.js)" в ООО Компания Стек


## Описание проекта

Это приложение для управления пользователями, построенное на Vue 3 с TypeScript и Vite. Основной компонент `UserTable` предоставляет полнофункциональный интерфейс для работы с пользователями, включая просмотр, редактирование, фильтрацию и управление данными.

## Основной компонент - UserTable

### Функциональность

#### 📊 **Отображение данных**
- Табличное представление пользователей с пагинацией
- Сортировка по колонкам (ID, имя, email, дата регистрации)
- Адаптивный дизайн с современным UI
- Отображение аватаров пользователей (с автогенерацией при отсутствии)

#### 🔍 **Фильтрация и поиск**
- Поиск по имени, email и ID пользователя
- Фильтрация по роли (администратор, пользователь, модератор)
- Фильтрация по статусу (активный, неактивный)
- Фильтрация по диапазону дат регистрации
- Быстрая очистка всех фильтров

#### ✏️ **Управление пользователями**
- Добавление новых пользователей через модальное окно
- Редактирование данных пользователей inline
- Удаление отдельных пользователей или группы выбранных
- Изменение статуса пользователя (активация/деактивация)
- Валидация форм с отображением ошибок

#### 📋 **Дополнительные возможности**
- Массовое выделение пользователей
- Экспорт данных в CSV формат
- Детальный просмотр информации о пользователе
- Статистика активности пользователей
- Состояния загрузки и обработки ошибок

### Архитектура компонента

#### Структура файлов:
```
src/components/users/
├── UserTable.vue              # Основной компонент-контейнер
├── UserTableHeader.vue        # Заголовок с поиском и кнопками
├── UserTableFilters.vue       # Панель фильтров
├── UserTableBody.vue          # Тело таблицы с данными
├── UserTableRow.vue           # Строка таблицы
├── UserTablePagination.vue    # Компонент пагинации
├── AddUserModal.vue           # Модальное окно добавления
└── UserDetailsModal.vue       # Модальное окно деталей
```

#### Composable функция:
- `useUserTable.ts` - содержит всю бизнес-логику управления состоянием

#### Типы данных:
- `user.ts` - TypeScript интерфейсы для типизации данных

### Технические особенности

#### Состояние и реактивность:
- Использует Composition API с `<script setup>`
- Реактивное состояние через `ref` и `computed`
- Provide/Inject для передачи состояния между компонентами

#### Производительность:
- Виртуализация больших списков через пагинацию
- Debounced поиск для оптимизации
- Ленивая загрузка данных

#### UX/UI:
- Индикаторы загрузки и состояний
- Анимации и переходы
- Адаптивный дизайн
- Accessibility поддержка

## Клонирование и запуск проекта

### Предварительные требования

- **Node.js** версии 16.0 или выше
- **npm** или **yarn** пакетный менеджер
- **Git** для клонирования репозитория

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <URL_РЕПОЗИТОРИЯ>
   cd Test_task_vue_3_TS
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   # или
   yarn install
   ```

### Запуск в режиме разработки

```bash
npm run dev
# или
yarn dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
# или
yarn build
```

### Предварительный просмотр сборки

```bash
npm run preview
# или
yarn preview
```

## Технологический стек

- **Vue 3** - прогрессивный JavaScript фреймворк
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый инструмент сборки
- **Composition API** - новый API для создания компонентов
- **CSS3** - современные стили с Flexbox и Grid

## Структура проекта

```
├── public/                 # Статические файлы
├── src/
│   ├── components/         # Vue компоненты
│   │   └── users/         # Компоненты управления пользователями
│   ├── composables/       # Переиспользуемая логика
│   ├── types/             # TypeScript типы
│   ├── App.vue           # Корневой компонент
│   ├── main.ts           # Точка входа
│   └── style.css         # Глобальные стили
├── package.json          # Зависимости и скрипты
├── tsconfig.json         # Конфигурация TypeScript
├── vite.config.ts        # Конфигурация Vite
└── README.md            # Документация
```

## Особенности реализации

### Управление состоянием
Используется паттерн Composable для инкапсуляции логики управления пользователями. Все состояние централизовано в `useUserTable` composable.

### Типизация
Полная типизация с TypeScript обеспечивает безопасность типов и лучший DX (Developer Experience).

### Модульность
Компонент разбит на логические части, что обеспечивает переиспользуемость и легкость тестирования.

### Производительность
Используются оптимизации Vue 3, включая реактивность на основе Proxy и эффективный алгоритм обновления DOM.

## Возможности расширения

- Интеграция с реальным API
- Добавление роутинга для детальных страниц
- Реализация системы уведомлений
- Добавление тестов (unit, integration)
- Интернационализация (i18n)
- Темизация и кастомизация UI